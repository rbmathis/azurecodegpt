
export type Settings = {
    graphUri: string; 
    vaultUri: string; 
    selectedInsideCodeblock?: boolean;
    pasteOnClick?: boolean;
    model?: string;
    maxTokens?: number;
    temperature?: number;
};